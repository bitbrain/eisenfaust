# aoc-eisenfaust.de website

This page hosts the aoc-eisenfaust.de website via Github Pages.

## Stack

Static site generation via `vite-ssg`, powered by `Vue 3` and `PrimeVue`.

## Development

```bash
npm install
npm run dev
```